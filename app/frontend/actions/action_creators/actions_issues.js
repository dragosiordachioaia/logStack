import * as thisFile from "./actions_issues"; // eslint-disable-line no-restricted-syntax

import { FETCH_ISSUES } from "actions/action_types";

export function fetchIssues() {
  return (dispatch, getState) => {
    // let state = getState();
    // let evtLabel = args[0];
    // if (state.analytics.sentEvents.indexOf(evtLabel) === -1) {
    //   dispatch(thisFile.setAnalyticsEvent(...args));
    // }
  };
}
